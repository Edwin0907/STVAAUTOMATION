/**
 * 
 */
package ctva_rest_api_client;

/**
 * @author velmurugan_k02
 *
 */
/*public class CTVA_API_Calls {

	public static String serviceUrl = "", symphonyService="", baseUrl="", userName="", passWord="", authenticationToken = "";
	*//**
	 * @param args
	 *//*
	public static void main(String[] args) {
		
		serviceUrl = System.getenv("AuthenticationService");
		symphonyService = System.getenv("SymphonyService");
		baseUrl = System.getenv("PROD");
		userName = System.getenv("Username");
		passWord = System.getenv("Password");
		CTVA_Request_Helper helper = new CTVA_Request_Helper(userName, passWord);
		authenticationToken = helper.GetAuthenticationToken(serviceUrl);
		if(authenticationToken!=null && !authenticationToken.isEmpty())
		{
			helper.ExecuteCTVARequest(baseUrl, symphonyService, authenticationToken, HttpVerb.GET);
		}
		System.exit(0);
	}

}*/
